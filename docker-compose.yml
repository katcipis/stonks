version: '3.8'

services:
    dev:
        build:
          context: .
          dockerfile: ./hack/Dockerfile.dev
          args:
            GOVERSION: "${goversion}"
        volumes:
            - .:/app
        ports:
            - "8080:8080"
        depends_on:
            - usersdb

    usersdb:
      build:
          context: ./hack
          dockerfile: ./Dockerfile.usersdb
